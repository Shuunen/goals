{
  "$schema": "https://json.schemastore.org/package",
  "author": "Romain Racamier-Lafon <romain.racamier@gmail.com>",
  "bugs": "https://github.com/Shuunen/goals/issues",
  "dependencies": {
    "shuutils": "7.2"
  },
  "description": "web app to visualize & reach goals",
  "devDependencies": {
    "@types/mocha": "10",
    "@types/node": "18",
    "c8": "7",
    "dependency-cruiser": "latest",
    "esbuild": "0.17",
    "eslint": "8.38",
    "eslint-config-hardcore": "26",
    "eslint-plugin-unicorn": "46",
    "npm-parallel": "2",
    "repo-check": "1.30",
    "servor": "4",
    "tsm": "2.3",
    "typescript": "4.9",
    "typescript-run": "0",
    "uvu": "0.5",
    "watchlist": "0.3"
  },
  "files": [
    "src",
    "public"
  ],
  "homepage": "https://github.com/Shuunen/goals",
  "keywords": [
    "webapp",
    "goals",
    "list"
  ],
  "license": "GPL-3.0",
  "name": "@shuunen/goals",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/Shuunen/goals.git"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outdir=public --format=iife && echo build success",
    "build:watch": "watchlist src -eager -- npm run build",
    "check": "repo-check",
    "ci": "npm run build && npm run lint && npm run check && npm run test && echo ci success",
    "dev": "parallel serve build:watch",
    "lint": "tsc --noEmit && eslint --cache --fix --ignore-path .gitignore --ext .js,.ts . && depcruise src --config && echo lint success",
    "mark": "unique-mark public/index.html",
    "postversion": "git push && git push --tags",
    "preversion": "npm run ci",
    "serve": "servor public --reload",
    "test": "c8 uvu -r tsm tests && echo test success",
    "test:watch": "watchlist src tests -eager -- npm test"
  },
  "version": "0.1.0"
}
